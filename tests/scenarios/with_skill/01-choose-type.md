# Post-skill scenario: choosing a question type

With the bruv skill loaded, the agent selects `bruv noul "Does this ask for a
refund?" --state <message> --output json` and reads `answers.q1.noul` while
preserving the calibration marker.